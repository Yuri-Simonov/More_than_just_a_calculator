// Цепочка дней
export const days = [];
for (let i = 1; i <= 31; i++) {
    days.push(i);
}

// Цепочка месяцев
export const months = [];
for (let i = 1; i <= 12; i++) {
    months.push(i);
}

// Цепочка годов
export const years = [];
for (let i = 1900; i <= 2100; i++) {
    years.push(i);
}
